# SQL document: `zuluia_back_terceros_full_upgrade`

Migrado desde `zuluia_back_terceros_full_upgrade.sql` para ejecución manual sobre PostgreSQL local.

```sql
BEGIN;

-- ─────────────────────────────────────────────────────────────────────────────
-- TERCEROS: PERSONERÍA Y DATOS FISCALES BASE
-- ─────────────────────────────────────────────────────────────────────────────
ALTER TABLE IF EXISTS terceros
    ADD COLUMN IF NOT EXISTS tipo_personeria character varying(20) NOT NULL DEFAULT 'JURIDICA';

ALTER TABLE IF EXISTS terceros
    ADD COLUMN IF NOT EXISTS nombre character varying(150);

ALTER TABLE IF EXISTS terceros
    ADD COLUMN IF NOT EXISTS apellido character varying(150);

ALTER TABLE IF EXISTS terceros
    ADD COLUMN IF NOT EXISTS tratamiento character varying(250);

ALTER TABLE IF EXISTS terceros
    ADD COLUMN IF NOT EXISTS profesion character varying(250);

ALTER TABLE IF EXISTS terceros
    ADD COLUMN IF NOT EXISTS estado_civil_id bigint;

ALTER TABLE IF EXISTS terceros
    ADD COLUMN IF NOT EXISTS estado_civil character varying(250);

ALTER TABLE IF EXISTS terceros
    ADD COLUMN IF NOT EXISTS nacionalidad character varying(250);

ALTER TABLE IF EXISTS terceros
    ADD COLUMN IF NOT EXISTS sexo character varying(1);

ALTER TABLE IF EXISTS terceros
    ADD COLUMN IF NOT EXISTS fecha_nacimiento date;

ALTER TABLE IF EXISTS terceros
    ADD COLUMN IF NOT EXISTS fecha_registro date;

ALTER TABLE IF EXISTS terceros
    ADD COLUMN IF NOT EXISTS porcentaje_maximo_descuento numeric(5,2);

ALTER TABLE IF EXISTS terceros
    ADD COLUMN IF NOT EXISTS es_entidad_gubernamental boolean NOT NULL DEFAULT FALSE;

ALTER TABLE IF EXISTS terceros
    ADD COLUMN IF NOT EXISTS clave_fiscal character varying(50);

ALTER TABLE IF EXISTS terceros
    ADD COLUMN IF NOT EXISTS valor_clave_fiscal character varying(30);

-- ─────────────────────────────────────────────────────────────────────────────
-- CATÁLOGOS LEGACY DE CLIENTES / PROVEEDORES
-- ─────────────────────────────────────────────────────────────────────────────
CREATE TABLE IF NOT EXISTS categorias_clientes (
    id bigint GENERATED BY DEFAULT AS IDENTITY,
    codigo character varying(20) NOT NULL,
    descripcion character varying(100) NOT NULL,
    activa boolean NOT NULL DEFAULT TRUE,
    created_at timestamp with time zone NOT NULL DEFAULT now(),
    updated_at timestamp with time zone NOT NULL DEFAULT now(),
    deleted_at timestamp with time zone,
    created_by bigint,
    updated_by bigint,
    CONSTRAINT pk_categorias_clientes PRIMARY KEY (id)
);

CREATE UNIQUE INDEX IF NOT EXISTS ix_categorias_clientes_codigo
    ON categorias_clientes (codigo);

CREATE TABLE IF NOT EXISTS categorias_proveedores (
    id bigint GENERATED BY DEFAULT AS IDENTITY,
    codigo character varying(20) NOT NULL,
    descripcion character varying(100) NOT NULL,
    activa boolean NOT NULL DEFAULT TRUE,
    created_at timestamp with time zone NOT NULL DEFAULT now(),
    updated_at timestamp with time zone NOT NULL DEFAULT now(),
    deleted_at timestamp with time zone,
    created_by bigint,
    updated_by bigint,
    CONSTRAINT pk_categorias_proveedores PRIMARY KEY (id)
);

CREATE UNIQUE INDEX IF NOT EXISTS ix_categorias_proveedores_codigo
    ON categorias_proveedores (codigo);

CREATE TABLE IF NOT EXISTS estados_clientes (
    id bigint GENERATED BY DEFAULT AS IDENTITY,
    codigo character varying(20) NOT NULL,
    descripcion character varying(100) NOT NULL,
    bloquea boolean NOT NULL DEFAULT FALSE,
    activo boolean NOT NULL DEFAULT TRUE,
    created_at timestamp with time zone NOT NULL DEFAULT now(),
    updated_at timestamp with time zone NOT NULL DEFAULT now(),
    deleted_at timestamp with time zone,
    created_by bigint,
    updated_by bigint,
    CONSTRAINT pk_estados_clientes PRIMARY KEY (id)
);

CREATE UNIQUE INDEX IF NOT EXISTS ix_estados_clientes_codigo
    ON estados_clientes (codigo);

CREATE TABLE IF NOT EXISTS estados_proveedores (
    id bigint GENERATED BY DEFAULT AS IDENTITY,
    codigo character varying(20) NOT NULL,
    descripcion character varying(100) NOT NULL,
    bloquea boolean NOT NULL DEFAULT FALSE,
    activo boolean NOT NULL DEFAULT TRUE,
    created_at timestamp with time zone NOT NULL DEFAULT now(),
    updated_at timestamp with time zone NOT NULL DEFAULT now(),
    deleted_at timestamp with time zone,
    created_by bigint,
    updated_by bigint,
    CONSTRAINT pk_estados_proveedores PRIMARY KEY (id)
);

CREATE UNIQUE INDEX IF NOT EXISTS ix_estados_proveedores_codigo
    ON estados_proveedores (codigo);

CREATE TABLE IF NOT EXISTS estados_personas (
    id bigint GENERATED BY DEFAULT AS IDENTITY,
    descripcion character varying(100) NOT NULL,
    activo boolean NOT NULL DEFAULT TRUE,
    created_at timestamp with time zone NOT NULL DEFAULT now(),
    updated_at timestamp with time zone NOT NULL DEFAULT now(),
    deleted_at timestamp with time zone,
    created_by bigint,
    updated_by bigint,
    CONSTRAINT pk_estados_personas PRIMARY KEY (id)
);

CREATE UNIQUE INDEX IF NOT EXISTS ux_estados_personas_descripcion
    ON estados_personas (descripcion);

CREATE TABLE IF NOT EXISTS estados_civiles (
    id bigint GENERATED BY DEFAULT AS IDENTITY,
    descripcion character varying(100) NOT NULL,
    activo boolean NOT NULL DEFAULT TRUE,
    created_at timestamp with time zone NOT NULL DEFAULT now(),
    updated_at timestamp with time zone NOT NULL DEFAULT now(),
    deleted_at timestamp with time zone,
    created_by bigint,
    updated_by bigint,
    CONSTRAINT pk_estados_civiles PRIMARY KEY (id)
);

CREATE UNIQUE INDEX IF NOT EXISTS ux_estados_civiles_descripcion
    ON estados_civiles (descripcion);

ALTER TABLE IF EXISTS terceros
    ADD COLUMN IF NOT EXISTS categoria_cliente_id bigint;

ALTER TABLE IF EXISTS terceros
    ADD COLUMN IF NOT EXISTS estado_cliente_id bigint;

ALTER TABLE IF EXISTS terceros
    ADD COLUMN IF NOT EXISTS pais_id bigint;

ALTER TABLE IF EXISTS terceros
    ADD COLUMN IF NOT EXISTS vigencia_credito_desde date;

ALTER TABLE IF EXISTS terceros
    ADD COLUMN IF NOT EXISTS vigencia_credito_hasta date;

ALTER TABLE IF EXISTS terceros
    ADD COLUMN IF NOT EXISTS categoria_proveedor_id bigint;

ALTER TABLE IF EXISTS terceros
    ADD COLUMN IF NOT EXISTS estado_proveedor_id bigint;

ALTER TABLE IF EXISTS terceros
    ADD COLUMN IF NOT EXISTS aplica_comision_cobrador boolean NOT NULL DEFAULT FALSE;

ALTER TABLE IF EXISTS terceros
    ADD COLUMN IF NOT EXISTS aplica_comision_vendedor boolean NOT NULL DEFAULT FALSE;

ALTER TABLE IF EXISTS terceros
    ADD COLUMN IF NOT EXISTS usuario_id bigint;

CREATE INDEX IF NOT EXISTS ix_terceros_categoria_cliente_id
    ON terceros (categoria_cliente_id);

CREATE INDEX IF NOT EXISTS ix_terceros_estado_cliente_id
    ON terceros (estado_cliente_id);

CREATE INDEX IF NOT EXISTS ix_terceros_estado_civil_id
    ON terceros (estado_civil_id);

CREATE INDEX IF NOT EXISTS ix_terceros_estado_persona_id
    ON terceros (estado_persona_id);

CREATE INDEX IF NOT EXISTS ix_terceros_pais_id
    ON terceros (pais_id);

CREATE INDEX IF NOT EXISTS ix_terceros_provincia_id
    ON terceros (provincia_id);

CREATE INDEX IF NOT EXISTS ix_terceros_categoria_proveedor_id
    ON terceros (categoria_proveedor_id);

CREATE INDEX IF NOT EXISTS ix_terceros_estado_proveedor_id
    ON terceros (estado_proveedor_id);

CREATE INDEX IF NOT EXISTS ix_terceros_usuario_id
    ON terceros (usuario_id);

CREATE TABLE IF NOT EXISTS tipodomicilio (
    id bigint GENERATED BY DEFAULT AS IDENTITY,
    tipodomicilio character varying(100) NOT NULL,
    CONSTRAINT pk_tipodomicilio PRIMARY KEY (id)
);

CREATE UNIQUE INDEX IF NOT EXISTS ux_tipodomicilio_tipodomicilio
    ON tipodomicilio (tipodomicilio);

INSERT INTO tipodomicilio (id, tipodomicilio)
SELECT 1, 'Principal'
WHERE NOT EXISTS (
    SELECT 1
    FROM tipodomicilio
    WHERE id = 1
       OR lower(trim(tipodomicilio)) = 'principal');

SELECT setval(
    pg_get_serial_sequence('tipodomicilio', 'id'),
    COALESCE((SELECT MAX(id) FROM tipodomicilio), 1),
    true);

CREATE TABLE IF NOT EXISTS per_domicilio (
    dom_id bigint GENERATED BY DEFAULT AS IDENTITY,
    per_id bigint NOT NULL,
    tdom_id bigint,
    prov_id bigint,
    loc_id bigint,
    dom_domicilio character varying(200),
    dom_barrio character varying(100),
    dom_cp character varying(20),
    dom_observacion character varying(500),
    dom_orden integer NOT NULL DEFAULT 0,
    dom_defecto boolean NOT NULL DEFAULT FALSE,
    CONSTRAINT pk_per_domicilio PRIMARY KEY (dom_id),
    CONSTRAINT fk_per_domicilio_terceros_per_id FOREIGN KEY (per_id) REFERENCES terceros (id) ON DELETE CASCADE
);

CREATE INDEX IF NOT EXISTS ix_per_domicilio_per_id
    ON per_domicilio (per_id);

CREATE INDEX IF NOT EXISTS ix_per_domicilio_per_id_dom_defecto
    ON per_domicilio (per_id, dom_defecto);

CREATE INDEX IF NOT EXISTS ix_per_domicilio_per_id_dom_orden
    ON per_domicilio (per_id, dom_orden);

CREATE INDEX IF NOT EXISTS ix_per_domicilio_tdom_id
    ON per_domicilio (tdom_id);

CREATE INDEX IF NOT EXISTS ix_per_domicilio_prov_id
    ON per_domicilio (prov_id);

CREATE INDEX IF NOT EXISTS ix_per_domicilio_loc_id
    ON per_domicilio (loc_id);

DO $$
BEGIN
    IF NOT EXISTS (
        SELECT 1
        FROM pg_constraint
        WHERE conname = 'fk_per_domicilio_tipodomicilio_tdom_id') THEN
        ALTER TABLE per_domicilio
            ADD CONSTRAINT fk_per_domicilio_tipodomicilio_tdom_id
            FOREIGN KEY (tdom_id) REFERENCES tipodomicilio (id) ON DELETE RESTRICT;
    END IF;
END $$;

DO $$
BEGIN
    IF NOT EXISTS (
        SELECT 1
        FROM pg_constraint
        WHERE conname = 'fk_per_domicilio_provincias_prov_id') THEN
        ALTER TABLE per_domicilio
            ADD CONSTRAINT fk_per_domicilio_provincias_prov_id
            FOREIGN KEY (prov_id) REFERENCES provincias (id) ON DELETE RESTRICT;
    END IF;
END $$;

DO $$
BEGIN
    IF NOT EXISTS (
        SELECT 1
        FROM pg_constraint
        WHERE conname = 'fk_per_domicilio_localidades_loc_id') THEN
        ALTER TABLE per_domicilio
            ADD CONSTRAINT fk_per_domicilio_localidades_loc_id
            FOREIGN KEY (loc_id) REFERENCES localidades (id) ON DELETE RESTRICT;
    END IF;
END $$;

DO $$
BEGIN
    IF NOT EXISTS (
        SELECT 1
        FROM pg_constraint
        WHERE conname = 'fk_terceros_provincias_provincia_id') THEN
        ALTER TABLE terceros
            ADD CONSTRAINT fk_terceros_provincias_provincia_id
            FOREIGN KEY (provincia_id) REFERENCES provincias (id) ON DELETE RESTRICT;
    END IF;
END $$;

DO $$
BEGIN
    IF NOT EXISTS (
        SELECT 1
        FROM pg_constraint
        WHERE conname = 'fk_terceros_categorias_clientes_categoria_cliente_id') THEN
        ALTER TABLE terceros
            ADD CONSTRAINT fk_terceros_categorias_clientes_categoria_cliente_id
            FOREIGN KEY (categoria_cliente_id) REFERENCES categorias_clientes (id) ON DELETE RESTRICT;
    END IF;
END $$;

DO $$
BEGIN
    IF NOT EXISTS (
        SELECT 1
        FROM pg_constraint
        WHERE conname = 'fk_terceros_estados_personas_estado_persona_id') THEN
        ALTER TABLE terceros
            ADD CONSTRAINT fk_terceros_estados_personas_estado_persona_id
            FOREIGN KEY (estado_persona_id) REFERENCES estados_personas (id) ON DELETE RESTRICT;
    END IF;
END $$;

DO $$
BEGIN
    IF NOT EXISTS (
        SELECT 1
        FROM pg_constraint
        WHERE conname = 'fk_terceros_usuarios_usuario_id') THEN
        ALTER TABLE terceros
            ADD CONSTRAINT fk_terceros_usuarios_usuario_id
            FOREIGN KEY (usuario_id) REFERENCES usuarios (id) ON DELETE RESTRICT;
    END IF;
END $$;

DO $$
BEGIN
    IF NOT EXISTS (
        SELECT 1
        FROM pg_constraint
        WHERE conname = 'fk_terceros_estados_civiles_estado_civil_id') THEN
        ALTER TABLE terceros
            ADD CONSTRAINT fk_terceros_estados_civiles_estado_civil_id
            FOREIGN KEY (estado_civil_id) REFERENCES estados_civiles (id) ON DELETE RESTRICT;
    END IF;
END $$;

DO $$
BEGIN
    IF NOT EXISTS (
        SELECT 1
        FROM pg_constraint
        WHERE conname = 'fk_terceros_paises_pais_id') THEN
        ALTER TABLE terceros
            ADD CONSTRAINT fk_terceros_paises_pais_id
            FOREIGN KEY (pais_id) REFERENCES paises (id) ON DELETE RESTRICT;
    END IF;
END $$;

DO $$
BEGIN
    IF NOT EXISTS (
        SELECT 1
        FROM pg_constraint
        WHERE conname = 'fk_terceros_estados_clientes_estado_cliente_id') THEN
        ALTER TABLE terceros
            ADD CONSTRAINT fk_terceros_estados_clientes_estado_cliente_id
            FOREIGN KEY (estado_cliente_id) REFERENCES estados_clientes (id) ON DELETE RESTRICT;
    END IF;
END $$;

DO $$
BEGIN
    IF NOT EXISTS (
        SELECT 1
        FROM pg_constraint
        WHERE conname = 'fk_terceros_categorias_proveedores_categoria_proveedor_id') THEN
        ALTER TABLE terceros
            ADD CONSTRAINT fk_terceros_categorias_proveedores_categoria_proveedor_id
            FOREIGN KEY (categoria_proveedor_id) REFERENCES categorias_proveedores (id) ON DELETE RESTRICT;
    END IF;
END $$;

DO $$
BEGIN
    IF NOT EXISTS (
        SELECT 1
        FROM pg_constraint
        WHERE conname = 'fk_terceros_estados_proveedores_estado_proveedor_id') THEN
        ALTER TABLE terceros
            ADD CONSTRAINT fk_terceros_estados_proveedores_estado_proveedor_id
            FOREIGN KEY (estado_proveedor_id) REFERENCES estados_proveedores (id) ON DELETE RESTRICT;
    END IF;
END $$;

-- ─────────────────────────────────────────────────────────────────────────────
-- PERFIL COMERCIAL AMPLIADO
-- ─────────────────────────────────────────────────────────────────────────────
CREATE TABLE IF NOT EXISTS terceros_perfiles_comerciales (
    id bigint GENERATED BY DEFAULT AS IDENTITY,
    tercero_id bigint NOT NULL,
    zona_comercial_id bigint,
    rubro character varying(100),
    subrubro character varying(100),
    sector character varying(100),
    condicion_cobranza character varying(150),
    riesgo_crediticio character varying(20) NOT NULL DEFAULT 'NORMAL',
    saldo_maximo_vigente numeric(18,4),
    vigencia_saldo character varying(100),
    vigencia_saldo_desde date,
    vigencia_saldo_hasta date,
    condicion_venta character varying(150),
    plazo_cobro character varying(150),
    facturador_por_defecto character varying(150),
    minimo_factura_mipymes numeric(18,4),
    observacion_comercial character varying(1000),
    created_at timestamp with time zone NOT NULL DEFAULT now(),
    updated_at timestamp with time zone NOT NULL DEFAULT now(),
    deleted_at timestamp with time zone,
    created_by bigint,
    updated_by bigint,
    CONSTRAINT pk_terceros_perfiles_comerciales PRIMARY KEY (id),
    CONSTRAINT fk_terceros_perfiles_comerciales_terceros_tercero_id FOREIGN KEY (tercero_id) REFERENCES terceros (id) ON DELETE CASCADE,
    CONSTRAINT fk_terceros_perfiles_comerciales_zonas_comerciales_zona_comercial_id FOREIGN KEY (zona_comercial_id) REFERENCES zonas_comerciales (id) ON DELETE RESTRICT
);

CREATE UNIQUE INDEX IF NOT EXISTS ix_terceros_perfiles_comerciales_tercero_id
    ON terceros_perfiles_comerciales (tercero_id);

CREATE INDEX IF NOT EXISTS ix_terceros_perfiles_comerciales_zona_comercial_id
    ON terceros_perfiles_comerciales (zona_comercial_id);

-- ─────────────────────────────────────────────────────────────────────────────
-- CONTACTOS MÚLTIPLES
-- ─────────────────────────────────────────────────────────────────────────────
CREATE TABLE IF NOT EXISTS terceros_contactos (
    id bigint GENERATED BY DEFAULT AS IDENTITY,
    tercero_id bigint NOT NULL,
    nombre character varying(200) NOT NULL,
    cargo character varying(100),
    email character varying(150),
    telefono character varying(50),
    sector character varying(100),
    principal boolean NOT NULL DEFAULT FALSE,
    orden integer NOT NULL DEFAULT 0,
    created_at timestamp with time zone NOT NULL DEFAULT now(),
    updated_at timestamp with time zone NOT NULL DEFAULT now(),
    deleted_at timestamp with time zone,
    created_by bigint,
    updated_by bigint,
    CONSTRAINT pk_terceros_contactos PRIMARY KEY (id),
    CONSTRAINT fk_terceros_contactos_terceros_tercero_id FOREIGN KEY (tercero_id) REFERENCES terceros (id) ON DELETE CASCADE
);

CREATE INDEX IF NOT EXISTS ix_terceros_contactos_tercero_orden
    ON terceros_contactos (tercero_id, orden);

CREATE INDEX IF NOT EXISTS ix_terceros_contactos_tercero_principal
    ON terceros_contactos (tercero_id, principal);

-- ─────────────────────────────────────────────────────────────────────────────
-- SUCURSALES / PUNTOS DE ENTREGA
-- ─────────────────────────────────────────────────────────────────────────────
CREATE TABLE IF NOT EXISTS terceros_sucursales_entrega (
    id bigint GENERATED BY DEFAULT AS IDENTITY,
    tercero_id bigint NOT NULL,
    descripcion character varying(200) NOT NULL,
    direccion character varying(300),
    localidad character varying(150),
    responsable character varying(150),
    telefono character varying(50),
    horario character varying(150),
    principal boolean NOT NULL DEFAULT FALSE,
    orden integer NOT NULL DEFAULT 0,
    created_at timestamp with time zone NOT NULL DEFAULT now(),
    updated_at timestamp with time zone NOT NULL DEFAULT now(),
    deleted_at timestamp with time zone,
    created_by bigint,
    updated_by bigint,
    CONSTRAINT pk_terceros_sucursales_entrega PRIMARY KEY (id),
    CONSTRAINT fk_terceros_sucursales_entrega_terceros_tercero_id FOREIGN KEY (tercero_id) REFERENCES terceros (id) ON DELETE CASCADE
);

CREATE INDEX IF NOT EXISTS ix_terceros_sucursales_entrega_tercero_orden
    ON terceros_sucursales_entrega (tercero_id, orden);

CREATE INDEX IF NOT EXISTS ix_terceros_sucursales_entrega_tercero_principal
    ON terceros_sucursales_entrega (tercero_id, principal);

-- ─────────────────────────────────────────────────────────────────────────────
-- TRANSPORTES
-- ─────────────────────────────────────────────────────────────────────────────
CREATE TABLE IF NOT EXISTS terceros_transportes (
    id bigint GENERATED BY DEFAULT AS IDENTITY,
    tercero_id bigint NOT NULL,
    transportista_id bigint,
    nombre character varying(200) NOT NULL,
    servicio character varying(150),
    zona character varying(150),
    frecuencia character varying(150),
    observacion character varying(500),
    activo boolean NOT NULL DEFAULT TRUE,
    principal boolean NOT NULL DEFAULT FALSE,
    orden integer NOT NULL DEFAULT 0,
    created_at timestamp with time zone NOT NULL DEFAULT now(),
    updated_at timestamp with time zone NOT NULL DEFAULT now(),
    deleted_at timestamp with time zone,
    created_by bigint,
    updated_by bigint,
    CONSTRAINT pk_terceros_transportes PRIMARY KEY (id),
    CONSTRAINT fk_terceros_transportes_terceros_tercero_id FOREIGN KEY (tercero_id) REFERENCES terceros (id) ON DELETE CASCADE,
    CONSTRAINT fk_terceros_transportes_transportistas_transportista_id FOREIGN KEY (transportista_id) REFERENCES transportistas (id) ON DELETE RESTRICT
);

CREATE INDEX IF NOT EXISTS ix_terceros_transportes_tercero_orden
    ON terceros_transportes (tercero_id, orden);

CREATE INDEX IF NOT EXISTS ix_terceros_transportes_tercero_principal
    ON terceros_transportes (tercero_id, principal);

CREATE INDEX IF NOT EXISTS ix_terceros_transportes_transportista_id
    ON terceros_transportes (transportista_id);

-- ─────────────────────────────────────────────────────────────────────────────
-- VENTANAS DE COBRANZA
-- ─────────────────────────────────────────────────────────────────────────────
CREATE TABLE IF NOT EXISTS terceros_ventanas_cobranza (
    id bigint GENERATED BY DEFAULT AS IDENTITY,
    tercero_id bigint NOT NULL,
    dia character varying(50) NOT NULL,
    franja character varying(100),
    canal character varying(100),
    responsable character varying(150),
    principal boolean NOT NULL DEFAULT FALSE,
    orden integer NOT NULL DEFAULT 0,
    created_at timestamp with time zone NOT NULL DEFAULT now(),
    updated_at timestamp with time zone NOT NULL DEFAULT now(),
    deleted_at timestamp with time zone,
    created_by bigint,
    updated_by bigint,
    CONSTRAINT pk_terceros_ventanas_cobranza PRIMARY KEY (id),
    CONSTRAINT fk_terceros_ventanas_cobranza_terceros_tercero_id FOREIGN KEY (tercero_id) REFERENCES terceros (id) ON DELETE CASCADE
);

CREATE INDEX IF NOT EXISTS ix_terceros_ventanas_cobranza_tercero_orden
    ON terceros_ventanas_cobranza (tercero_id, orden);

CREATE INDEX IF NOT EXISTS ix_terceros_ventanas_cobranza_tercero_principal
    ON terceros_ventanas_cobranza (tercero_id, principal);

COMMIT;

```
